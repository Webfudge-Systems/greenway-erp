# Railway config when Root Directory = apps/api (recommended for Greenway-Backend)

[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && npm run build --workspace=@greenways/api"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/admin"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
